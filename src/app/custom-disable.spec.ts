import { CustomDisable } from './custom-disable';

describe('CustomDisable', () => {
  it('should create an instance', () => {
    const directive = new CustomDisable();
    expect(directive).toBeTruthy();
  });
});
